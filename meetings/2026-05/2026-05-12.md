# Development Meeting Summary

**Date:** May 12, 2026

---

## Reward System Design

The team enumerated the distinct reward streams the Roko protocol is designed to support: staking rewards, block rewards, gas rewards (collected when a validator builds a block), and transaction timestamping rewards. The timestamping reward was highlighted as a differentiator with no equivalent on other chains — it is intended to incentivize validators to process and timestamp transactions as quickly as possible and to compete on providing that service, which in turn distributes network load and gives validators a direct stake in driving traction. The specific reward values remain configurable and are still under discussion. There was a proposal to publish the tokenomics ideas publicly on social channels to invite outside debate and gather community insight.

## Explorer and Indexer Work

Work continued on the block explorer's indexer to surface more time-related features and strengthen the network's time-focused differentiation. A key gap discussed was that the existing Blockscout-based explorer operated only on the EVM side, leaving substrate-side data unindexed; unifying both into a single explorer view is the main objective here.

## Node Services and Time Attestation

The team is tightening the API that nodes expose to the outside world, notably for timestamping and time attestation — the capability for any validator to attest the time of arbitrary data. The underlying logic is already wired at the implementation level, but the public API does not yet expose it; that work is in progress. A related open item is locating prior code snippets for detecting the time hardware a validator is running on, so that hardware detection can be included in a future delivery.

## Timing Hardware Test Bed

A new single-board compute module paired with a dedicated timing board carrying its own oscillator is being set up as an improved test bed for validating the timing software. The hardware is expected to provide roughly 24-hour holdover with microsecond drift, enabling A/B comparison against a second clock and against NTP. The goal is to determine whether the software can reliably distinguish better timing hardware from the noise floor alone; results will inform whether the detection approach is integrated or remains theoretical pending access to higher-grade reference clocks. Bringing the board online is currently waiting on a power adapter.

## Validator Provisioning

The team discussed standing up a new live validator using artifacts from an existing validator to streamline setup. Provisioning can be handled either through the Roko admin interface or via a helper script with worked examples for creating staking entries and minting test tokens. All required configuration already resides on the existing device, so the new validator can be brought up without additional setup work.

## Conference Opportunity

A large industry conference in Warsaw scheduled for September was discussed as a strong outreach opportunity, with a complimentary speaker slot and booth already offered to the team. Sending at least one team member was treated as worthwhile; the open question is funding for travel and accommodation, which the team plans to resolve closer to the date.

---

## Next Steps

- Continue building out time-focused explorer and indexer features, unifying EVM and substrate data.
- Expose the timestamping and time-attestation API to external callers.
- Recover and integrate time-hardware detection snippets in a future delivery.
- Complete setup of the new timing hardware test bed once the power adapter arrives, then run A/B and NTP comparisons.
- Confirm conference attendance and work out a travel funding plan ahead of September.
- Prepare a consolidated tokenomics write-up for the following session.
